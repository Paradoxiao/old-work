#include <stdio.h>
#include <string.h>
char a[28299999999];
int main() {
  memset(a, 0, sizeof(a));
  // int a[10] = {1, 2, 3};
  // for (int i = 0; i < 10; i++)
  //  printf("%d ", a[i]);
  return 0;
}
