#include <bits/stdc++.h>
#include <iostream>
#include <vector>
using namespace std;
int main() {
  int n, m, k;
  cin >> n >> m >> k;
  auto matrix = vector<vector<int>>(n, vector<int>(n, 0));

  return 0;
}